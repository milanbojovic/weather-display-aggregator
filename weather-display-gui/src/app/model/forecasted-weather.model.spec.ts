import { ForecastedWeather } from './forecasted-weather.model';

describe('Forecasted weather', () => {
  it('should create an instance', () => {
    expect(new ForecastedWeather()).toBeTruthy();
  });
});
